---
deployment:
  tasks:
    - export DEPLOYPATH=/home/busnisscard/public_html/fit
    - /bin/cp -R * $DEPLOYPATH
    - /bin/cp .env.example $DEPLOYPATH/.env
    - cd $DEPLOYPATH
    - php artisan key:generate --force
    - php artisan storage:link --force
    - php artisan migrate --force
